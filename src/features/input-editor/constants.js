export const INDEX_COLUMN = 'name';

/**
 * Why a row is tinted and missing from the map.
 *
 * Shown twice -- as the row's hover text and in the banner above the table -- so it lives here
 * rather than being written out in both places and drifting apart.
 */
export const NO_GEOMETRY_REASON =
  'has no footprint, so it is not drawn on the map.';
export const NO_GEOMETRY_FIX_ONE = 'Give it a geometry or delete the row.';
export const NO_GEOMETRY_FIX_MANY = 'Give them a geometry or delete them.';
